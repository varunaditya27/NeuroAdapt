# gen-engine Python Dependencies
# Tested: Python 3.11 / 3.12 — clean install verified April 2026

# ── Core Framework ──────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.6

# ── HTTP / LLM Integration ───────────────────────────────────────
requests==2.32.3
tenacity==9.0.0
ollama==0.4.1

# ── Text Processing ──────────────────────────────────────────────
textstat==0.7.3
spacy==3.8.2
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# ── Image Generation ─────────────────────────────────────────────
# torch MUST be pinned before diffusers/transformers to avoid resolver conflicts
torch==2.4.1                     # 2.5.0 has a known metadata conflict with transformers 4.46.x
torchvision==0.19.1              # must match torch minor version exactly
transformers==4.46.3             # 4.46.0 has a tokenizer regression; use .3
diffusers==0.30.3                # 0.31.0 requires transformers>=4.47 which conflicts above
accelerate==1.1.1                # required by diffusers pipeline; omitting causes runtime crash
Pillow==10.4.0                   # Pillow 11.x breaks torchvision's image transforms

# ── Audio ────────────────────────────────────────────────────────
pydub==0.25.1
numpy==1.26.4                    # numpy 2.x breaks torch 2.4.x C extensions at runtime
manim==0.18.1

# ── Database ─────────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Observability ────────────────────────────────────────────────
prometheus-client==0.21.0
python-dotenv==1.0.1
psutil==6.0.0

# ── Testing ──────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
